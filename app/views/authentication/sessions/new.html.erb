<section class="bg-gray-50 min-h-screen flex items-center justify-center bg-cover bg-center" style="background-image: url('assets/wallpaper.jpg');">
  <!-- Login container -->
  <div class="bg-gray-100 flex flex-col sm:flex-row rounded-2xl shadow-lg max-w-3xl p-5">
    <!-- Form -->
    <div class="sm:w-1/2 px-8 sm:px-16">
      <h2 class="font-bold text-2xl text-teal-500 mb-4">
        Login
      </h2>
      <p class="text-sm mb-4">
        Inicia sesión para acceder a todas nuestras características 😊
      </p>

      <%= form_with(url: sessions_path) do |form| %>
        <div class="p-2 rounded-xl border mb-4">
          <%= form.label :Usuario %>
          <%= form.text_field :login, required: true, class: "w-full border-gray-300 focus:border-teal-500 focus:ring focus:ring-teal-500 focus:ring-opacity-50 rounded-md" %>
        </div>

        <div class="p-2 rounded-xl border mb-4">
          <%= form.label :Contraseña %>
          <%= form.password_field :password, required: true, class: "w-full border-gray-300 focus:border-teal-500 focus:ring focus:ring-teal-500 focus:ring-opacity-50 rounded-md" %>
        </div>

        <div class="flex justify-center">
          <%= form.submit "Iniciar sesión", class: "bg-teal-500 text-white px-4 py-2 rounded-md hover:bg-teal-600" %>
        </div>
      <% end %>
    </div>

    <!-- Imagen -->
    <div class="hidden sm:flex items-center justify-center w-1/2">
      <img class="rounded-2xl" src="assets/login.jpg" alt="">
    </div>
  </div>
</section>
