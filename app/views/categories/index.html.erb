<h1 class="text-2xl font-bold"><%= t('.title') %></h1>

<div id="categories" class="mt-4">
  <%= render partial: 'category', collection: @categories %>
</div>

<%= link_to t('.new'), new_category_path, class: "bg-teal-400 hover:bg-teal-500 text-white font-bold py-2 px-4 rounded-md mt-4" %>
